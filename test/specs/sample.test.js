const Dial = require("../../pageObjects/Dial")
const dial = new Dial();

describe("Sample", () => {
  it("should verify", () => {
    dial.appBtn.click;
    // driver.pause(10000);
    dial.alertBtn.addValue;
  });
});
